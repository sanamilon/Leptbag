import std.stdio;
import std.math;
import std.random;
import std.conv;
import std.algorithm;

import dlib.math.vector;
import dlib.math.matrix;
import dlib.math.quaternion;

import functions;
import cma;


void main(){
	auto nyao = Matrix!(float, 3)();
	nyao[] = 0.0;
	writeln(nyao);
}

